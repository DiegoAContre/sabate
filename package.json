{
  "name": "sabate",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "db:generate": "npm run db:generate -w packages/db",
    "db:migrate": "npm run db:migrate -w packages/db",
    "db:push": "npm run db:push -w packages/db",
    "db:studio": "npm run db:studio -w packages/db",
    "seed": "npm run seed -w apps/api",
    "test": "npm run test -w apps/api",
    "test:db-setup": "docker compose exec -T db psql -U sabate -d postgres -tc \"SELECT 1 FROM pg_database WHERE datname = 'sabate_test'\" | grep -q 1 || docker compose exec -T db createdb -U sabate sabate_test && DATABASE_URL=postgres://sabate:sabate@localhost:5432/sabate_test npm run db:push"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "nodemon": "^3.1.14",
    "typescript": "^5.6.0"
  }
}
