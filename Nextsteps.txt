
3. Admin user management
- API: GET /api/admin/users (list), PATCH /api/admin/users/:id (role, isActive). requireRole('admin') guard.
- Web: app/admin/users/page.tsx with a toggle/edit UI, link in the admin layout.
4. Admin order management
- API: GET /api/admin/orders (all), PATCH /api/admin/orders/:id/status. Add listAllOrders/updateOrderStatus to orderService.
- Web: app/admin/orders/page.tsx (list + status dropdown) and optional detail page.
5. Polish / bug bash
A few real gaps I noticed while reading:
- Stock can go negative: handleCheckoutCompleted does stock - quantity with no floor/cap. Race-prone. Add a guard (greatest(0, ...) or reject if insufficient at checkout).
- Checkout doesn't verify price server-side — trusts cart price (fine for now, but note it).
- Delete product with existing orderItems uses onDelete: set null, so history survives — good, no action needed.
- No linter (only typecheck) — AGENTS.md already flags this as "add when codebase grows".